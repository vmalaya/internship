package com.github.vmalaya.sigmasoftware.internship.datastructures.map;

/**
 * {@link java.util.WeakHashMap} is useful to save resource,
 * so you can keep a reference to something.
 * Also you can use a WeakHashMap to reduce
 * the chance of a memory leak as a result of caching some object.
 * WeakHashMap is a  is a hashtable-based implementation of the Map
 * interface, with keys that are of a {@link java.lang.ref.WeakReference} type.
 */
public class WeakHashMapTest {
}
