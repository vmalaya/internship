package com.github.vmalaya.sigmasoftware.internship.datastructures.iterable.collection.queue;

/**
 * {@link java.util.concurrent.ConcurrentLinkedQueue}
 * is a general-purpose and efficient non-blocking queue.
 * It is faster then BlockingQueue but it does not deal with blocking empty Queue that leads to problems.
 * So you can use it if your queue is non expandable and contains only one producer/consumer thread.
 */
public class ConcurrentLinkedQueueTest {
}
